# ═══════════════════════════════════════════════════════════════════════════
# Plan & Joy - Kubernetes Ingress
# NGINX Ingress with cert-manager TLS
# Generated by Opsera Code-to-Cloud v4.0
# ═══════════════════════════════════════════════════════════════════════════
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: plan-and-joy
  labels:
    app: plan-and-joy
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - opsera-plan-and-joy-dev.agent.opsera.dev
      secretName: plan-and-joy-tls
  rules:
    - host: opsera-plan-and-joy-dev.agent.opsera.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: plan-and-joy
                port:
                  number: 80
