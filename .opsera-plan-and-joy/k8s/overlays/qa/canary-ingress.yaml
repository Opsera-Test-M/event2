# ═══════════════════════════════════════════════════════════════════════════
# Plan & Joy - Canary Ingress
# Used during progressive deployment for traffic splitting
# Generated by Opsera Code-to-Cloud v4.0
# ═══════════════════════════════════════════════════════════════════════════
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: plan-and-joy-canary
  namespace: plan-and-joy-qa
  labels:
    app: plan-and-joy
    environment: qa
    deployment-type: canary
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/canary: "true"
    nginx.ingress.kubernetes.io/canary-weight: "0"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - opsera-plan-and-joy-qa.agent.opsera.dev
      secretName: plan-and-joy-qa-tls
  rules:
    - host: opsera-plan-and-joy-qa.agent.opsera.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: plan-and-joy-canary
                port:
                  number: 80
