# ═══════════════════════════════════════════════════════════════════════════
# Plan & Joy - Canary Deployment
# Separate deployment for canary pods during progressive rollout
# Generated by Opsera Code-to-Cloud v4.0
# ═══════════════════════════════════════════════════════════════════════════
apiVersion: apps/v1
kind: Deployment
metadata:
  name: plan-and-joy-canary
  namespace: plan-and-joy-qa
  labels:
    app: plan-and-joy
    version: canary
    environment: qa
spec:
  replicas: 1
  selector:
    matchLabels:
      app: plan-and-joy
      version: canary
  template:
    metadata:
      labels:
        app: plan-and-joy
        version: canary
        environment: qa
    spec:
      containers:
        - name: plan-and-joy
          image: plan-and-joy:canary
          ports:
            - containerPort: 8080
              protocol: TCP
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
          startupProbe:
            httpGet:
              path: /
              port: 8080
            failureThreshold: 30
            periodSeconds: 2
