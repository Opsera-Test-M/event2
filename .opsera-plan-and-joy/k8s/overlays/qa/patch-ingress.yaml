# ═══════════════════════════════════════════════════════════════════════════
# Plan & Joy - QA Ingress Patch
# QA subdomain with TLS
# Generated by Opsera Code-to-Cloud v4.0
# ═══════════════════════════════════════════════════════════════════════════
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: plan-and-joy
  labels:
    environment: qa
  annotations:
    # Canary annotations (disabled by default, enabled during rollout)
    # nginx.ingress.kubernetes.io/canary: "true"
    # nginx.ingress.kubernetes.io/canary-weight: "0"
spec:
  tls:
    - hosts:
        - opsera-plan-and-joy-qa.agent.opsera.dev
      secretName: plan-and-joy-qa-tls
  rules:
    - host: opsera-plan-and-joy-qa.agent.opsera.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: plan-and-joy
                port:
                  number: 80
