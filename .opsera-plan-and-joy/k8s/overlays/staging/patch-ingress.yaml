# ═══════════════════════════════════════════════════════════════════════════
# Plan & Joy - Staging Ingress Patch
# Staging subdomain with TLS
# Generated by Opsera Code-to-Cloud v4.0
# ═══════════════════════════════════════════════════════════════════════════
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: plan-and-joy
  labels:
    environment: staging
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - opsera-plan-and-joy-staging.agent.opsera.dev
      secretName: plan-and-joy-staging-tls
  rules:
    - host: opsera-plan-and-joy-staging.agent.opsera.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: plan-and-joy
                port:
                  number: 80
