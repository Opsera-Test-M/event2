# ═══════════════════════════════════════════════════════════════════════════
# Plan & Joy - Dev Ingress Patch
# CRITICAL: TLS section MUST be included to override base TLS config
# Generated by Opsera Code-to-Cloud v4.0
# ═══════════════════════════════════════════════════════════════════════════
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: plan-and-joy
  labels:
    environment: dev
spec:
  tls:
    - hosts:
        - PLACEHOLDER_SUBDOMAIN
      secretName: plan-and-joy-dev-tls
  rules:
    - host: PLACEHOLDER_SUBDOMAIN
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: plan-and-joy
                port:
                  number: 80
