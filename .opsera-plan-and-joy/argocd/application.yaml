# ═══════════════════════════════════════════════════════════════════════════
# Plan & Joy - ArgoCD Application
# Generated by Opsera Code-to-Cloud v4.0
# ═══════════════════════════════════════════════════════════════════════════
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: plan-and-joy-dev
  namespace: argocd
  labels:
    app: plan-and-joy
    environment: dev
    app.kubernetes.io/managed-by: opsera
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: PLACEHOLDER_REPO_URL
    targetRevision: main
    path: .opsera-plan-and-joy/k8s/overlays/dev

  destination:
    name: opsera-usw2-np
    namespace: plan-and-joy-dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  revisionHistoryLimit: 10
