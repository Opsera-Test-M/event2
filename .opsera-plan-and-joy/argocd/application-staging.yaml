# ═══════════════════════════════════════════════════════════════════════════
# Plan & Joy - ArgoCD Application (Staging)
# GitOps deployment for Staging environment with Blue-Green support
# Generated by Opsera Code-to-Cloud v4.0
# Learning: SKILL-HUB-SPOKE-001
# ═══════════════════════════════════════════════════════════════════════════
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: plan-and-joy-staging
  namespace: argocd
  labels:
    app: plan-and-joy
    environment: staging
    tenant: opsera
    deployment-strategy: blue-green
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/Opsera-Test-M/event2.git
    targetRevision: main
    path: .opsera-plan-and-joy/k8s/overlays/staging
  destination:
    # SKILL-HUB-SPOKE-001: Use cluster NAME for spoke cluster
    name: opsera-usw2-np
    namespace: plan-and-joy-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  revisionHistoryLimit: 10
